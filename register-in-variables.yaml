---
- name: register keyword in variable
  groups: all
  become: yes
  tasks:
    - name: simple task
      shell: ls -ltr
      register: lsCommand
    - name: printing the previous command output
      debug:
        msg: "{{lsCommand}}"